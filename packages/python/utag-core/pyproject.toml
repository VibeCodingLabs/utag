[project]
name = "utag-core"
version = "2.3.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.13,<3", "pydantic-settings>=2.14,<3", "PyYAML>=6,<7"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/utag_core"]
