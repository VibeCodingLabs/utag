# Progress log: v2.16.0 — utag-factory

## 2026-07-15 — all six phases shipped

Built the internal automation factory the operator specified (daemonized
autoscaling runners, Redis Streams queue, sqlite-vec state, sandboxed workers,
Pydantic+router agents, NL input, Flash burst) inside the utag monorepo.

- **F0**: AutoscalingProfile schema family; operator template imported +
  promoted draft→canonical; supervisor go→python decision recorded.
- **F1**: JobStore/VectorStore, StreamQueue (all six streams, XAUTOCLAIM, DLQ),
  exact+semantic caches, retry taxonomy. 16 offline tests.
- **F2**: bwrap/process sandboxes (real isolation verified), worker loop with
  honest retries + DLQ, run-python + generate-artifact kinds. 8 tests.
- **F3**: AST-safe scaling formula, guardrails, daemonization. 7 tests.
- **F4/F5**: NL→JobRequest agent, Typer + FastAPI (SSE firehose) + FastMCP. 7 tests.
- **F6**: Flash cloud_burst tool catalog, capability-gated. 2 tests.
- Suite: **835 passed, 17 skipped** (+76 factory tests); 14/14 gates; determinism
  golden unchanged (`df6c2bbf…`) since the factory adds no generators.

Design decision (recorded in Specs/v2.16.0-factory.md): the operator asked for
better-sqlite3 (Node) — implemented as Python sqlite-vec over stdlib sqlite3
(WAL) so the whole factory is one language; and Typer/FastAPI/FastMCP for the
daemon surfaces per the operator's stack choice.

Fast-follow (not stubbed): weights volume + vLLM streaming endpoint — the
"download model weights and stream code execution/tool calls/chat" piece.
