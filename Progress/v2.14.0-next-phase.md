# Progress log: v2.14.0 next phase

## 2026-07-14 — Phase 0: Reconcile reality

- Baseline recorded at `77017ba` (branch `competitive/parity`, v2.11.0):
  **16 failed, 358 passed, 17 skipped** — contradicting the autoresearch
  report's "294 passed, 100% pass rate" and the handoff's "300+".
- Root cause of all 16 failures: missing `import json` in
  `packages/python/utag-generators/src/utag_generators/targets/multi_language_sdk.py`.
  Fixed; suite now **374 passed, 17 skipped, 0 failed**.
- Removed dead duplicate `cmd_intel` in `utag_cli/main.py`.
- Replaced the failure-swallowing harness with repo-root `autoresearch.sh`
  supporting `--strict` (fails on pytest failure) and `--compare`
  (metrics-only, never fails). Testable via `PYTEST_CMD` override.
- Added `scripts/check_entrypoints.py` (79 generators / 9 validators /
  8 importers reachability) and `scripts/run_quality_gate.py`
  (pytest + entrypoints required; ruff optional with explicit SKIP note).
- Added `tests/unit/test_autoresearch_harness.py` and
  `tests/integration/test_entrypoints.py`.
- Documented the 8 importers in README (public API via
  `utag_core.registry.get_importer`); they previously had no doc or CLI route.
- Flagged fake-feature debt: `utag openapi` subcommands are stub prints.
- Work is on branch `feat/v2.14.0-phase0`; remote push of `competitive/parity`
  is blocked by branch protection (GH013 — PRs only), and the repo moved to
  `VibeCodingLabs/utag` (origin URL updated).
- PR opened: https://github.com/VibeCodingLabs/utag/pull/5 (into `competitive/parity`).

## 2026-07-14 — Phase 1: Strict schema contract layer

- `utag_core.schemas`: 54 strict contracts in six modules (core, autoresearch,
  design, observability, ai, mcp). All `extra="forbid"` with an explicit
  `extensions` escape hatch; slug/sha256 patterns; enums for closed vocabularies.
- Deterministic JSON Schema 2020-12 emission (`$id`, sorted keys) to `schemas/`;
  108 fixtures (valid + invalid per kind) in `fixtures/schemas/`.
- `utag schema list|emit|validate|validate-all|doctor` CLI; doctor writes
  `reports/schema-validation/doctor.md`.
- Canonical `design.yaml` (utag-console) added; validates against `DesignYaml`.
- `utag generate` now writes `artifact.manifest.json` (per-file sha256, bytes,
  provenance with inputs hash) validated by construction against `ArtifactManifest`.
- 280 new tests (strictness/emission/fixtures/CLI, parametrized per kind).
  Full suite: **660 passed, 17 skipped, 0 failed**.
- Deferred: runtime `utag_core.report.ValidationReport` unification with the
  schema contract lands with observability run/span ids (Phase 4).

## 2026-07-14 — Phase 2: Registry manifest enforcement

- `register_generator/validator/importer` accept `manifest=`; unmigrated
  registrations get honest derived manifests (generators/validators cite the
  golden + conformance suites that parametrize over every registered id;
  importers cite `tests/unit/test_p2_importers.py`).
- `utag registry list|doctor|manifest --id|coverage --out` — doctor fails when
  a manifest lacks entrypoints or its test files are missing on disk; coverage
  report byte-deterministic.
- 10 new tests. Note: pushing to existing branches is impossible under the
  current ruleset (PR-required on ~ALL refs, no bypass) — phases accumulate
  locally and ship as a fresh branch + superseding PR (#6 currently open).

## 2026-07-14 — Phase 3: UDC design.yaml UI pipeline

- Generators `design-tokens-css` / `tailwind-v4-theme` / `react-component-library`
  registered with explicit manifests; automatically covered by the golden
  determinism matrix. Input: `module.provenance["design_yaml"]` validated
  against the strict `DesignYaml` contract.
- CSS variable families `--utag-{color,space,radius,font,shadow,motion}-*`;
  Tailwind v4 `@theme` maps `--color-*`/`--spacing-*`/`--font-*` onto them;
  dark theme emitted as both `data-theme` and `prefers-color-scheme`;
  `motion.css` ships the `prefers-reduced-motion` guard.
- TSX generation: typed props from ComponentSpec, state unions, semantic
  elements + `aria-label` everywhere, `tabIndex` per the design's
  `keyboardRequired` contract, colors only via `var(--utag-*)`.
- `utag design validate|tokens|components|app|snapshot`; snapshot report is
  byte-deterministic. Generated UI materialized at `packages/ui/src/` (14 files);
  `check_generated_ui.py` fails on drift or hardcoded colors.
- tsc typecheck test is capability-gated (repo convention: TS2307 tolerated in
  scratch dirs, ambient react shim; fully-resolved typecheck lands in Phase 8).
- Full suite: **690 passed, 17 skipped, 0 failed**.
- Deliberately deferred: the 11 other named UDC generators (dashboard/browser/
  explorer surfaces) are `planned`, tracked in TODO — not shipped as stubs.

## 2026-07-14 — Phase 4: Observability & run evidence

- `utag_core.observe.Recorder`: nested spans with parent ids, events, metrics —
  every record is a strict-schema instance; JSONL envelopes
  (`{"kind", "record"}`) validated line-by-line by `validate_jsonl`.
- `utag generate` wraps the pipeline in `utag.run`/`utag.generate`/`utag.validate`
  spans, records `utag_runs_total`/`utag_artifacts_generated_total`/
  `utag_run_duration_ms`/`utag_validation_failures_total`, flushes evidence to
  the store, and writes `validation.report.json` (contract `ValidationReport`
  with `run_id` + `span_id`) beside `artifact.manifest.json` — the Phase 1
  deferral is closed.
- `utag observe run|export|summary|doctor` + three scripts; doctor fails on any
  record that doesn't validate. Tests keep evidence in tmp via an autouse
  conftest fixture. No secrets recorded: attributes are only target/path/count
  strings the CLI already prints.
- Full suite: **697 passed, 17 skipped, 0 failed**.
- Deferred: `utag.ai.call`/`utag.mcp.tool.call`/worker spans wire in with
  Phases 5+ (the schema contracts already exist); UI evidence rendering is
  Phase 8.
