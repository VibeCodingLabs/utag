# Progress log: v2.15.0

## 2026-07-15 — all four workstreams shipped

- **A (generators)**: `typescript-contract-types` + `design-fixtures` added;
  `react-component-library` renders real contract-bound data; component-type
  vocabulary supersedes the 11 planned dashboard generators (decision in
  `Specs/v2.15.0.md`); interactions generated from design.yaml; `DesignYaml`
  gained `data.contracts`; OpenApi contracts added (57 schemas, 114 fixtures).
- **B (console shell)**: Vite+React app over 100% generated UI; real strict
  tsc + vite build green; ui-typecheck/ui-build gates required when the
  toolchain exists; ui-build automation + workflow (bijection kept).
- **C (openapi)**: stub prints retired; real pipeline + petstore fixtures +
  content-verifying tests; every subcommand records run evidence.
- **D (AI)**: RoutedStructuredAdapter (telemetry, budgets, cache),
  ai_bridge.port_for, Recorder.record(), `utag ai call` with credential
  refusal; 12 offline tests incl. FunctionModel through the real port.
- **E (release)**: version 2.15.0; 14 required gates PASS; determinism
  double-hash green (`df6c2bbf…`); `reports/v2.15.0/verification.md`.
- Suite: **775 passed, 17 skipped, 0 failed** (from 733 at the v2.14.0 merge).
