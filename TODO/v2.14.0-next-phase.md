# TODO: v2.14.0 next phase

## Phase 0 — Reconcile reality
- [x] Run full suite without swallowing failures; record baseline in Specs
- [x] Fix root cause of 16 failures (`import json` in multi_language_sdk.py)
- [x] Remove dead duplicate `cmd_intel`
- [x] `autoresearch.sh --strict` / `--compare` (repo-root, testable via PYTEST_CMD)
- [x] `scripts/check_entrypoints.py` + `scripts/run_quality_gate.py`
- [x] `tests/unit/test_autoresearch_harness.py`, `tests/integration/test_entrypoints.py`
- [x] README/AGENTS.md reflect real command surface and test count
- [ ] Historical-zip metric replay in `--compare` (zips carry no stored output; needs unzip+sync per zip)
- [ ] `utag openapi` subcommands are stub prints — implement or mark experimental

## Phase 1 — Strict schema contract layer (`utag schema`)
- [ ] `utag_core.schemas` models + JSON Schema 2020-12 emission to `schemas/`
- [ ] Valid + invalid fixture per schema under `fixtures/schemas/`
- [ ] `utag schema list|emit|validate|validate-all|doctor`
- [ ] `scripts/generate_json_schemas.py`, `scripts/validate_schemas.py`,
      `scripts/check_schema_fixtures.py`, `scripts/check_no_unknown_schema_extensions.py`
- [ ] Tests: test_schema_models / test_schema_json_emit / test_schema_validation / test_schema_cli

## Phase 2 — Registry manifest enforcement (`utag registry`)
- [ ] Manifest metadata on register_generator/importer/validator
- [ ] `utag registry list|doctor|manifest|coverage`

## Phase 3 — UDC design.yaml UI pipeline (`utag design`)
## Phase 4 — Observability (`utag observe`)
## Phase 5 — AI integration + model router (`utag ai`)
## Phase 6 — Autoresearch task execution (`utag autoresearch`)
## Phase 7 — Automation manifests + GitHub workflows (`utag automation`)
## Phase 8 — UI/agent surfaces (screens from design.yaml)
## Phase 9 — Final release gate (`scripts/run_quality_gate.py --release v2.14.0`)

Phase 0 DoD note: CLI help currently lists `generate validate targets init login
models intel openapi`. `schema`, `design`, `observe`, `autoresearch` are added by
their own phases — no stub commands are shipped ahead of real implementations
(quality rule 1: no hidden/fake features).
