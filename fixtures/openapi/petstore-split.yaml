openapi: 3.1.0
info:
  title: Split Petstore
  version: 1.0.0
paths:
  /pets:
    get:
      operationId: listPets
      summary: List all pets
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                $ref: "petstore-shared.yaml#/Pet"
