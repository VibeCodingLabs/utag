openapi: 3.1.0
info:
  title: Flawed
  version: 0.1.0
paths:
  /things:
    get:
      responses:
        "500":
          description: always broken
components:
  schemas:
    Unused:
      type: object
      properties:
        x:
          type: string
